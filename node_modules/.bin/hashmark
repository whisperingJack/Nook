#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../hashmark/bin/hashmark" "$@"
  ret=$?
else 
  node  "$basedir/../hashmark/bin/hashmark" "$@"
  ret=$?
fi
exit $ret
