#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../svg-sprite-generator/bin/svg-sprite-generate" "$@"
  ret=$?
else 
  node  "$basedir/../svg-sprite-generator/bin/svg-sprite-generate" "$@"
  ret=$?
fi
exit $ret
